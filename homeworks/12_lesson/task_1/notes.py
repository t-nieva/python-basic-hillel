# reg_exp_pattern = r"<[^>]+>"

# r - "raw string", каже Python не обробляти \ як спецсимвол, а залишати його як є
# < — початок HTML-тегу.
# [^>] — будь-який символ, крім >.
# [^>]+ — один або більше символів, що не є >.
# > — кінець тегу.
